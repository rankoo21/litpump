# Copy to .env and fill in. NEVER commit .env.
# .env.example MUST contain placeholders only — never real keys.
PRIVATE_KEY=0x...
FEE_RECIPIENT=0x...
# CREATION_FEE in wei. e.g. 0.001 zkLTC = 1000000000000000
CREATION_FEE=0
